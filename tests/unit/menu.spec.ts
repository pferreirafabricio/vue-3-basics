import { shallowMount } from '@vue/test-utils';
import Menu from '@/components/Menu.vue';

describe('Menu.vue', () => {
  it('renders all the menu items', () => {
    const msg = 'new message';
    const wrapper = shallowMount(Menu);
    expect(true);
  });
});
